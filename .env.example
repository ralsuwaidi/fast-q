# Copy this file to `.env` and fill in real values. `.env` is gitignored.

# Default (unset) = sqlite:///database.db
# DATABASE_URL=sqlite:///database.db

# Supabase (Session Pooler — IPv4 compatible, supports Alembic DDL):
# DATABASE_URL=postgresql://postgres.andrszjlprdlnhboavgo:<YOUR-DB-PASSWORD>@aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres
